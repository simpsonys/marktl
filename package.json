{
  "name": "marktl",
  "version": "0.17.7",
  "description": "Obsidian plugin that exports Markdown notes to reviewed HTML and static web-book artifacts.",
  "main": "main.js",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/main.ts --bundle --external:obsidian --format=cjs --platform=node --target=es2018 --outfile=main.js",
    "export:webbook": "node scripts/export-web-book.mjs",
    "export:webbook:sample": "node scripts/export-web-book.mjs --source sample-notes --output html-exports/ysda-publisher --site-title 'YSDA Publisher' --site-description 'Reviewed Markdown notes published as a static web book.'"
  },
  "keywords": [
    "obsidian",
    "markdown",
    "html"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.7",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.28.0",
    "obsidian": "^1.5.12",
    "typescript": "^5.4.5"
  }
}
